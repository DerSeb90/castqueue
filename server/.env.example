# Copy to .env and adjust. Never commit .env.
CQ_DOMAIN=pods.seifert-online.eu
CQ_PUBLIC_URL=https://pods.seifert-online.eu
CQ_USERNAME=seb
# Plain password, or a bcrypt hash prefixed with "bcrypt:" (e.g. from `htpasswd -nbB x PASS`).
CQ_PASSWORD=change-me
CQ_REFRESH_MINUTES=30
CQ_LOG_LEVEL=info
